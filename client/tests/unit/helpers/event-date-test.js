import { eventDate } from '../../../helpers/event-date';
import { module, test } from 'qunit';

module('Unit | Helper | event date');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = eventDate(42);
  assert.ok(result);
});

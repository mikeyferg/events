<div class="hero-inner">
	<div class="hero-copy">
		<h1>Event Coyotes</h1>
		<p>Find events in San Francisco</p>
		{{#each cities as |city|}}
			{{#if city.events}}
	    	{{link-to city.name 'city' city}}
			{{/if}}
	  {{/each}}
	</div>
</div>

<section>
  {{!-- <img src={{model.image_url}} alt={{model.name}} data-photo-id={{model.slug}} /> --}}
  <article>
    Address: {{model.address}}
  </article>
  <aside class="">
    <h3 class="section-header">Events</h3>
    {{#each model.events as |event|}}
      {{event-card city=model.city event=event}}
    {{/each}}
  </aside>
</section>
